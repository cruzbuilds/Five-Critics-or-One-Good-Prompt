## swarm

**Verdict:** BLOCK
**Ran:** security-reviewer, docs-reviewer, infra-reviewer, test-reviewer, scope-reviewer
**Skipped:** none

Verdicts: security-reviewer WARN, docs-reviewer BLOCK, infra-reviewer WARN, test-reviewer BLOCK, scope-reviewer PASS. The scope PASS is vacuous: there is no `engagement/` or `docs/decisions/`, so there was nothing to check against.

### Blocking

**`README.md`**
- `README.md:30` [docs-reviewer] The setup step says "keep the defaults" for `JWT_SECRET`. It never says the secret signs the session cookie, must be at least 16 characters, and must be unique outside local dev. Remove "or keep the defaults" for `JWT_SECRET`, document those requirements, and give a generation command such as `openssl rand -base64 32`. security-reviewer raised the same root cause and rated it Should fix (see `.env.example:2` below).

**`.env.example`**
- `.env.example:1-2` [docs-reviewer] Neither variable has a comment saying what it is for, whether it is required, or what happens if it is missing. Add a one-line comment above each. `DATABASE_URL` should say it must match the `docker-compose.yml` credentials.

**`package.json`**
- `package.json:5` [test-reviewer] There is no test runner, no `test` script, and no test file in a 6,985-line app with auth boundaries. Add a runner (vitest fits the stack) and a `test` script, then cover the items below, starting with ownership scoping and the share token.

**`src/app/api/ideas/[id]/route.ts`, `outcome/route.ts`, `share/route.ts`**
- `src/app/api/ideas/[id]/route.ts:14,25,50` [test-reviewer] The ownership checks (`findFirst({ where: { id, userId: user.id } })` then 404) are the only thing stopping one user reading, editing or deleting another user's idea. The same pattern is at `outcome/route.ts:14` and `share/route.ts:15,38`. None of it is tested. Test that user B gets 404 on GET, PATCH, DELETE, outcome PATCH, and share POST and DELETE for user A's idea, and that the row is unchanged.
- `src/app/api/ideas/[id]/share/route.ts:18-22` [test-reviewer] Share token behavior is untested: reuse of the existing token, rotation on `regenerate: true`, and the `{}` fallback on a malformed body. Test all three.

**`src/app/api/share/[token]/route.ts`**
- `src/app/api/share/[token]/route.ts:13-35` [test-reviewer] The public endpoint has no test. Test that a valid token returns the idea without `userId`, `id` or `shareToken`, that an unknown token returns 404, and that a revoked token returns 404.

**`src/app/api/auth/register/route.ts`, `login/route.ts`**
- `src/app/api/auth/register/route.ts:19-25` and `src/app/api/auth/login/route.ts:20-27` [test-reviewer] The duplicate-email 409, the unknown-user 401 and the wrong-password 401 are all untested. Test each status code, and test that the two 401 messages are identical.

**`src/lib/auth.ts`**
- `src/lib/auth.ts:38-51` [test-reviewer] `verifySessionToken` returns `null` on a bad signature, an expired token, or a missing `sub` or `email`. `getSecretKey` throws on a missing or short secret. Neither is tested. Test a tampered token, an expired token, a token without `email`, and the short-secret throw.

**`src/proxy.ts`**
- `src/proxy.ts:7-20` [test-reviewer] The redirect of unauthenticated `/ideas` requests to `/login` is the outer auth gate and is untested. Test no cookie, a garbage cookie, a valid cookie, and that `/login` is not redirected.

**`src/lib/scoring.ts`, `calibration.ts`, `validation.ts`**
- `src/lib/scoring.ts:25-47` [test-reviewer] `totalScore`, `hindsightTotal` and `scoreDelta` are untested, including the `null` branches. Test all four hindsight scores present, one missing returning `null`, and delta as hindsight minus prediction.
- `src/lib/calibration.ts:15-32` [test-reviewer] `computeCalibration` is untested, including the empty `null` return, the `hindsightTotal != null` filter, and the averaging. Test empty, mixed, and a hand-computed set (deltas +2 and -1 give `avgTotalDelta` 0.5).
- `src/lib/validation.ts:3-47` [test-reviewer] The Zod schemas carry the 1-5 range contract the README advertises, and none is tested. Test that scores 0, 6 and 3.5 are rejected and 1 and 5 accepted, that email is lowercased and trimmed, and that `outcomeSchema` rejects `ACTIVE`.

### Should fix

**`.env.example` / `src/lib/auth.ts`**
- `.env.example:2` and `src/lib/auth.ts:10` [security-reviewer; also flagged by docs-reviewer and infra-reviewer as handoffs] The placeholder `JWT_SECRET` is 33 characters, so it passes the `< 16` guard. Anyone who copies the file unchanged signs sessions with a publicly known key and can forge a cookie for any user id. Reject the placeholder at startup or leave the variable empty so the guard fires. This is the same root cause as the blocking `README.md:30` finding.

**`docker-compose.yml`**
- `docker-compose.yml:9-10` [security-reviewer, infra-reviewer (as a handoff)] Postgres is published on all interfaces with the credentials `idealog/idealog`. Bind to `127.0.0.1:5432:5432`.
- `docker-compose.yml:4` [infra-reviewer] There is no `healthcheck`. `pnpm db:up` then `pnpm db:migrate` back to back can fail because Postgres isn't accepting connections yet. Add a `pg_isready` healthcheck and use `up -d --wait` in `db:up`.

**`package.json`**
- `package.json:10` and `README.md:20-36` [infra-reviewer, docs-reviewer] The only migration command is `prisma migrate dev`, a local-development command that can prompt or reset the database. Nothing says how migrations reach a non-local database. Add a `db:deploy` script running `prisma migrate deploy`. Also add a "Running in production" README section covering migrations, required env vars, and that `NODE_ENV=production` is what enables the `secure` cookie flag (`src/lib/session.ts:17`).
- `package.json:11` [infra-reviewer, docs-reviewer (Noted)] There is no teardown script for `db:up`, and `docker compose down` leaves the `idealog_pgdata` volume. Add a `db:down` script, or document `docker compose down -v` in the README.
- `package.json:1` [infra-reviewer] There is no `engines` field, though `README.md:23` claims Node 20+. Add one matching the Next 16.3.5 minimum. infra-reviewer did not check that version against the Next docs.
- `pnpm-lock.yaml` (path `prisma > @prisma/config > deepmerge-ts@7.1.5`) [security-reviewer] `pnpm audit` reports one high-severity advisory, GHSA-ggr8-5vv4-36mx (a stack-exhaustion crash). It is in Prisma CLI config loading and is not shown to be reachable from a request path. Bump prisma when a fixed release exists or add a pnpm override. Moving `prisma` to `devDependencies` would also keep the CLI out of the production tree.

**`src/app/api/auth/*`**
- `src/app/api/auth/login/route.ts:17` and `register/route.ts:23` [security-reviewer, docs-reviewer (as a handoff)] There is no rate limiting or lockout on login or register. With bcrypt cost 12, login is also a cheap CPU-exhaustion target. Add per-IP and per-account throttling.
- `src/app/api/auth/register/route.ts:23-28` [security-reviewer, docs-reviewer (as a handoff)] The 409 "An account with that email already exists" lets anyone enumerate registered emails. Return a neutral response or explicitly accept the tradeoff.

**`src/lib/session.ts`**
- `src/lib/session.ts:22-28` and `src/lib/auth.ts:8` [security-reviewer, docs-reviewer] Sessions are stateless 30-day JWTs. Logout only deletes the cookie (`src/app/api/auth/logout/route.ts:5-6`), so a stolen token stays valid until it expires, and a password change doesn't invalidate it. Shorten the TTL or add a `tokenVersion` on `User` checked in `getCurrentUser`. docs-reviewer also wants an ADR recording this choice and its cost.

**`README.md`**
- `README.md:1-60` [docs-reviewer; scope-reviewer noted the same gap] There is no `docs/decisions/` and no ADR for choices that are expensive to reverse. These are Postgres with Prisma, stateless JWT sessions, and CHECK constraints as the range guard. Add an ADR for the auth and session model at minimum.
- `README.md:1-60` [docs-reviewer] No limitations are stated: no tests, no password reset, no email verification, no rate limiting, no session revocation. Add a "Known limitations" section naming each.
- `README.md:44-51` [docs-reviewer] The "Key routes" table lists pages only. It omits the nine API endpoints, including the public `GET /api/share/[token]`, and the `{ regenerate: true }` option in `src/app/api/ideas/[id]/share/route.ts:10` is documented only in a code comment. Add an API table with method, path, auth requirement and purpose.
- `README.md:9` [docs-reviewer; also noticed by infra-reviewer and test-reviewer] "unshareable/unguessable link" contradicts the sharing feature. Reword to say the link is unguessable and anyone holding it can view the idea.

**`prisma/migrations/20260917213700_score_checks/migration.sql`**
- `prisma/migrations/20260917213700_score_checks/migration.sql:1-10` [docs-reviewer, test-reviewer] The CHECK constraints exist only in this hand-written migration and are not visible in `schema.prisma`. A squash or reset from the schema alone would silently drop them, and they are never exercised by a test. Add a comment in `schema.prisma` and a README note saying they must be preserved. Add one integration test that inserts a score of 6 directly and expects failure.

**`src/app/api/ideas/route.ts`, `outcome/route.ts`**
- `src/app/api/ideas/route.ts:13-25` [test-reviewer] The `status` filter silently ignores an invalid value and the list is re-sorted by `totalScore` descending. Test that `?status=BOGUS` returns all ideas, `?status=DONE` filters, and ordering is by total descending.
- `src/app/api/ideas/[id]/outcome/route.ts:26-37` [test-reviewer] `outcomeRecordedAt` is set from `new Date()`. Any test of it needs a controlled clock or a range assertion.

### Handoffs nobody picked up

- `src/app/api/ideas/[id]/route.ts:37-40` [noticed by test-reviewer, scope-reviewer did not report it] PATCH passes `parsed.data` straight to `update`. A caller can set `status` to `DONE` or `ABANDONED` with no outcome or hindsight scores, and can rewrite prediction scores after hindsight was recorded, which corrupts the calibration numbers. test-reviewer notes no agent in the roster owns this. scope-reviewer had no engagement document to compare against.
- `src/app/api/auth/login/route.ts:19-29` [noticed by test-reviewer, security-reviewer did not report it] The unknown-user path skips bcrypt, so response timing differs from the wrong-password path. This leaks whether an email is registered. security-reviewer reviewed this file and reported only the missing rate limit and the register-side 409.
- `README.md` [noticed by test-reviewer, docs-reviewer did not report it] There are no instructions for running tests. docs-reviewer covered the missing tests as a limitation but not the missing run instructions, which only matter once tests exist.

### Noted

- **Scope context is missing.** No `engagement/` or `docs/decisions/` exists. scope-reviewer's PASS means only that there was nothing to compare against. It lists these decisions as likely needing ADRs: Prisma with a migration-managed SQL store, the custom session and auth model, public token share links, and the scoring and calibration feature. The single commit is "raw generated build, unreviewed" (`b38c5b0`, about 6,985 lines across 60 files).
- **security-reviewer's tools.** gitleaks and semgrep (`p/security-audit`, `p/secrets`) ran clean, but semgrep covered `src` and `prisma` only. `pnpm audit` ran. `npm audit` couldn't run (no npm lockfile) and `pip-audit` is not installed (not applicable to Node).
- **infra-reviewer's tools.** `actionlint`, `tflint` and `hadolint` are not installed. `checkov` is installed but failed on `docker-compose.yml` with an SSL error reaching `api0.prismacloud.io`, so it produced no results. That review was a manual read.
- **test-reviewer's tools.** Nothing was run: there is no runner, no `scripts/check.sh` and no CI. `pnpm lint` is the project's only automated check.
- **docs-reviewer's tools.** `node_modules` is not installed, so it could not check the README's Next.js 16 claims against `node_modules/next/dist/docs/`.
- **No infra surface beyond local dev.** No CI workflows, IaC, Dockerfile or deploy scripts, so none of infra-reviewer's block conditions apply.
- **Cloud config.** No cloud identifiers are hardcoded, `.env*` is gitignored except `.env.example`, and `postgres:16-alpine` is pinned to a major tag.
- **Share routes.** `/api/share/[token]` and `/s/[token]` are public by design. The tokens are 24-character nanoids and the routes return no owner PII.
- **Session cookie.** It is `httpOnly`, `sameSite: lax`, and `secure` in production.
- **Proxy.** `src/proxy.ts` guards only `/ideas`, but pages and API routes re-check the session, so this is defense in depth.
- **Share regenerate.** The unvalidated `regenerate` body field at `src/app/api/ideas/[id]/share/route.ts:22` is harmless because the route is owner-scoped.
- **Ignored path.** `.claude/` was ignored as instructed.

### Noticed while merging

Nothing beyond the above.

<!-- swarm run: 164s, agents from a18fba7 -->
