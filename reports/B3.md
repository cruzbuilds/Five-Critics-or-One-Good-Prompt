## swarm

**Verdict:** BLOCK
**Ran:** security-reviewer, docs-reviewer, infra-reviewer, test-reviewer, scope-reviewer
**Skipped:** none

Individual verdicts: docs-reviewer BLOCK, test-reviewer BLOCK, security-reviewer WARN, infra-reviewer WARN, scope-reviewer PASS. The scope PASS is vacuous: there is no `engagement/` or `docs/decisions/` to hold the change against (see Noted).

### Blocking

**`README.md`**
- `README.md:27` [docs-reviewer; security-reviewer flagged the code side at `src/lib/auth.ts:9`] `JWT_SECRET` is documented as "fill in … (or keep the defaults)". The `.env.example` placeholder (33 characters) passes the `>= 16` length check, so an app run on the defaults signs sessions with a publicly known key. The README doesn't say what the variable is for, that it's required, or how to generate one. Add an env-var section and a one-line comment per entry in `.env.example`.
- `README.md:59` [docs-reviewer; scope-reviewer noted the same gap informationally] Several hard-to-reverse choices have no ADR, and `docs/decisions/` doesn't exist:
  - hand-rolled stateless 30-day JWT auth, where logout only clears the cookie;
  - bearer-token share links as the public read model;
  - Postgres with raw-SQL CHECK constraints for the 1-5 score range;
  - scores computed on read.

  Write at least one ADR for the auth/session model and one for share links.

**`package.json`**
- `package.json:6` [test-reviewer] There is no `test` script, no runner and no test files in the tree. This is an app with real logic and an authorization boundary, so nothing would catch a regression. Add a runner and a `test` script, then the tests below.

**`src/lib/`**
- `src/lib/scoring.ts:22` [test-reviewer] `totalScore`, `hindsightTotal` (28-40) and `scoreDelta` (42-47) are untested. Test that:
  - `totalScore({4,3,2,5})` returns 14;
  - `hindsightTotal` returns `null` unless all four hindsight fields are set;
  - `scoreDelta` is `hindsight - predicted` (positive and negative) and `null` on partial hindsight.
- `src/lib/calibration.ts:16` [test-reviewer] `computeCalibration` is untested, including the empty-input `null` branch, the `hindsight ?? predicted` fallback (22) and the `delta ?? 0` fallback (29). Test empty input, known deltas, and that ideas without hindsight are excluded from `count`.
- `src/lib/auth.ts:40` [test-reviewer] `verifySessionToken` returning `null` on tampered, expired or wrong-secret tokens is untested, as is `getSecretKey` throwing on a missing or short secret (7-13). Test a round trip, a flipped character, and an expired token.
- `src/lib/validation.ts:3` [test-reviewer] The 1-5 integer bound, the outcome schema and the update-schema optionality (30-40) are untested. Test that 0, 6, 2.5 and `"3"` are rejected, that `status: "ACTIVE"` and an empty outcome are rejected, and that `description` defaults to `""`.

**`src/proxy.ts`**
- `src/proxy.ts:10-16` [test-reviewer] The redirect-to-`/login` branch and the pass-through branch are untested. Call `proxy` on `/ideas` with no cookie and with a valid cookie.

**`src/app/api/`**
- `src/app/api/ideas/[id]/route.ts:14` [test-reviewer] Owner scoping (`findFirst({ where: { id, userId } })`) is the only thing keeping one user's ideas private, and it is untested. The same pattern is at lines 25 and 50 here, at `outcome/route.ts:14`, and at `share/route.ts:15` and `:38`. Test that user B gets 404 on GET, PATCH, DELETE, outcome, share and unshare against user A's idea, and that A's idea is unchanged afterwards.
- `src/app/api/ideas/[id]/share/route.ts:21` [test-reviewer] The share-token logic is untested:
  - a second POST reuses the token and `{regenerate:true}` rotates it (21-22);
  - DELETE clears the token (43);
  - a bad body falls back through `.catch(() => ({}))` (18).
- `src/app/api/share/[token]/route.ts:13` [test-reviewer] The unauthenticated read endpoint is untested. Test a valid token, unknown and revoked/rotated tokens (404), and that the response has no `userId` or `shareToken`. The hand-built field allow-list at 17-35 is what a later edit could widen.
- `src/app/api/auth/register/route.ts:20` and `src/app/api/auth/login/route.ts:20-27` [test-reviewer] The duplicate-email 409, the bad-input 400 and the two 401 branches (unknown user, wrong password) are untested, as is the cookie being set on success.
- `src/app/api/ideas/route.ts:13` [test-reviewer] The silent-ignore of an invalid `?status=` (13, 18) and the descending `totalScore` sort (23-25) are untested.
- `src/app/api/ideas/[id]/outcome/route.ts:26` [test-reviewer] Recording an outcome is untested. Assert that `status`, `outcome`, `outcomeRecordedAt` and the four `h*` scores persist and that the response `delta` matches.

### Should fix

**`README.md`**
- `README.md:10` [docs-reviewer] "unshareable/unguessable link" contradicts the sharing feature and could read as "sharing is disabled". docs-reviewer suggests "unguessable"; test-reviewer suggested "unlisted".
- `README.md:23` [docs-reviewer] Prerequisites omit pnpm, though `package.json` pins `pnpm@10.29.1` and every README command uses it.
- `README.md:46` [docs-reviewer] "Key routes" lists only pages. Add a table of the API routes with method, auth requirement and purpose, including the `regenerate` option that is documented only in a code comment at `share/route.ts:10`.
- `README.md:59` [docs-reviewer] There is no production or limitations section. It should say that `db:migrate` is `prisma migrate dev`, and list what is absent: login rate limiting, password reset and email verification, session revocation, and tests.

**`package.json` and `docker-compose.yml`**
- `package.json:12` [infra-reviewer] The only migration script is `prisma migrate dev`, a development command that can prompt and reset the database. Nothing runs `prisma migrate deploy`, so the two committed migrations have no working path onto a non-local database. Add a `db:deploy` script and document it.
- `docker-compose.yml:11-15` and `README.md:64` [infra-reviewer, docs-reviewer] Data lives in the named volume `idealog_pgdata`, and there is no way to remove it. `docker compose down` keeps the volume. Add a `db:down` script running `docker compose down -v`, or document the removal.
- `docker-compose.yml:9-10` [security-reviewer] Postgres is published on all host interfaces (`"5432:5432"`) with the credentials `idealog`/`idealog`. Bind it to `"127.0.0.1:5432:5432"`.

**`src/lib/auth.ts`**
- `src/lib/auth.ts:9` [security-reviewer; the README side is Blocking above] The secret check is length-only, so the `.env.example` placeholder passes. Reject the placeholder, at least when `NODE_ENV === "production"`.

**`src/app/api/auth/`**
- `src/app/api/auth/login/route.ts:7` and `src/app/api/auth/register/route.ts:7` [security-reviewer; docs-reviewer noted the same gap] There is no rate limiting or lockout. Login allows unlimited password guessing, and both routes run bcrypt at cost 12 (`auth.ts:21`) on unauthenticated requests, so they are a cheap CPU-exhaustion target. Add per-IP and per-account throttling.
- `src/app/api/auth/login/route.ts:19-22` [security-reviewer] The not-found path returns before any bcrypt work, so response timing reveals which emails are registered. Run `bcrypt.compare` against a dummy hash on that path.
- `src/app/api/auth/register/route.ts:19-25` [security-reviewer, docs-reviewer] The 409 "account already exists" response lets someone enumerate registered emails, and with no rate limit that can be done in bulk. Decide this deliberately, or return a generic response.
- `src/app/api/auth/register/route.ts:19` [test-reviewer] Find-then-create is racy. Two concurrent registrations for the same email make `user.create` throw on the unique constraint and return a 500 instead of the 409. Handle the constraint error and test it.

**Other**
- `prisma/migrations/20260917213700_score_checks/migration.sql:2` [test-reviewer] Nothing shows the eight CHECK constraints reject out-of-range values. Add a DB-level test inserting `differentiation = 6` and `hCost = 0`.
- `prisma/schema.prisma:33` [docs-reviewer] The 1-5 CHECK constraints appear only in the hand-written migration. Comment the score fields so nobody edits them without a matching migration.
- `src/app/s/[token]/page.tsx:35` [test-reviewer] The page recomputes the delta inline (`hTotal - total`) instead of calling `scoreDelta`. It renders hindsight when `outcome` is set even if the `h*` scores are `null` (72-88). Neither case is tested, nor is `notFound()` for a bad token.

### Handoffs nobody picked up
- `src/app/api/ideas/[id]/route.ts:37` [noticed by test-reviewer; no owner in the roster] `PATCH` accepts `status: "DONE"` or `"ABANDONED"` through `ideaUpdateSchema` with no outcome or hindsight scores, bypassing the outcome flow. This is a correctness/design question that no agent owns.

All other handoffs were reported by the owning agent and are dropped: the missing tests (test-reviewer), the JWT secret, credentials and rate limiting (security-reviewer), the README typo (docs-reviewer), and `restart: unless-stopped` (infra-reviewer). The missing `engagement/` and `docs/decisions/` folders were reported by scope-reviewer and docs-reviewer.

### Noted
- **Tools that ran:** none. No linter, scanner, test runner or coverage tool ran.
  - security-reviewer: gitleaks, semgrep and `pnpm audit` did not run, so its "no blocking issues" rests on a manual read only. It could not judge next 16.3.5, jose 6.x, nanoid 6 or zod 4.
  - infra-reviewer: Bash was denied, so checkov did not run; actionlint, tflint and hadolint are not installed and had nothing to check.
  - test-reviewer: no runner or `scripts/check.sh` exists. Its `git ls-files` check found no test files.
- **Git access:** Bash was denied at some point for security-reviewer, infra-reviewer, test-reviewer and scope-reviewer. security-reviewer says it could not confirm the diff range with git and read files directly. scope-reviewer ran only a `--stat` (60 files, 6,985 insertions) and did not run the full diff. Treat these reviews as reads of the working tree, not of the diff.
- **scope-reviewer's PASS means "no scope to check", not "checked and in scope".** It did not read every changed file. Re-run it once an `engagement/` folder exists.
- The commit message is "raw generated build, unreviewed". The dependencies (`@prisma/client`, `bcryptjs`, `jose`, `nanoid`, `next`, `prisma`, `react`, `react-dom`, `zod`) have no justification recorded anywhere except the Prisma 6.x pin at `README.md:61-63`.
- docs-reviewer could not verify the `src/proxy.ts` "Next middleware convention" claim against the bundled Next docs because `node_modules/` isn't installed. `AGENTS.md` says this Next version has breaking changes.
- `src/lib/auth.ts:44` [security-reviewer] `jwtVerify` has no `algorithms: ["HS256"]`. jose restricts by key type, so this is not exploitable today, but pinning is cheap.
- Sessions are stateless and last 30 days, and `getCurrentUser` re-checks that the user still exists (`session.ts:40`). The cookie is `httpOnly`, `sameSite: lax` and `secure` in production, which is adequate CSRF protection for the JSON routes.
- Share tokens are `nanoid(24)`. The public route and `/s/[token]` return only owner-written fields.
- `docker-compose.yml:4` `restart: unless-stopped` brings the dev database back after every reboot. `docker-compose.yml:3` uses the floating `postgres:16-alpine` tag, which is fine for dev.
- The README says Node 20+, but there is no `.nvmrc` or `engines` field. Build-script approval is configured in both `pnpm-workspace.yaml:1-3` and `package.json:37-42`, and the two can drift.
- The pure functions in `scoring.ts`, `calibration.ts` and `validation.ts` need no database and are the cheapest tests to add first.
- test-reviewer did not read `src/components/`.
- reviewers disagree on the placeholder secret length (33 vs 34 characters). Either way it exceeds the 16-character minimum.

<!-- swarm run: 169s, agents from 7461982 -->
